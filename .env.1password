# 1Password secret references for tunnel-server
# This file is safe to commit - it contains no actual secrets
#
# Usage:
#   op run --env-file=.env.1password -- python3 main.py
#
# Prerequisites:
#   1. Create a 1Password vault named "Tunnel"
#   2. Create an item named "tunnel-server" with these fields:
#      - jwt-secret: openssl rand -hex 32
#      - admin-password: your chosen password
#      - admin-token: openssl rand -hex 32
#   3. For servers: create a service account with access to the Tunnel vault
#      and set OP_SERVICE_ACCOUNT_TOKEN environment variable

JWT_SECRET=op://Tunnel/tunnel-server/jwt-secret
ADMIN_PASSWORD=op://Tunnel/tunnel-server/admin-password
ADMIN_TOKEN=op://Tunnel/tunnel-server/admin-token
DB_PATH=/var/lib/tunnel-server/tunnel.db

# Netlify DNS (for automatic DNS record creation on startup)
# Only the API token is required - zone ID is auto-detected from the domain
NETLIFY_API_TOKEN=op://Tunnel/tunnel-server/netlify-api-token
TUNNEL_DOMAIN=tunnel.ersantana.com
